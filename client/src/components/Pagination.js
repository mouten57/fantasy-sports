// import React, {Component} from 'react'
// import { Pagination } from 'semantic-ui-react'

// const PaginationEx = (props) => {

//     const handlePageChange = (event, data) => {
//        props.handlePageChange(data.activePage)
//     }
//         return <Pagination defaultActivePage={props.activePage} totalPages={props.totalPages} onPageChange={handlePageChange} />

// }

// export default PaginationEx
